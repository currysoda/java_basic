package enum_example;

public enum Shape {
	// enum 의 기본
	// enum 은 내부적으로 ordinal() 값으로 맨 앞에 적힌 것인 0 그 후 1씩 늘어난 정수로 저장
	Circle, Rectangle, triangle
	// enum 의 ordinal 예시
	//	triangle, Circle, Rectangle
}
